# Fleximy — Variables de entorno requeridas
# Copiar este archivo a `.env` y completar los valores reales.
# NUNCA commitear `.env`. Los valores de ejemplo van aquí, sin secretos.

# Supabase (área privada / dashboard)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
